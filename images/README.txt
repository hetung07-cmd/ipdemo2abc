THƯ MỤC ẢNH — Technology Transfer Handbook (bản SPA)
=====================================================

Bỏ 11 file ảnh sau vào ĐÚNG thư mục này (images/), giữ NGUYÊN tên
(phân biệt hoa/thường phần mở rộng .JPG / .jpg / .webp):

TT Phase Flow (hiện ở mục Description của mỗi phase):
  P0   -> IPtransfermodel.JPG
  P1a  -> TechnologytransferP1.JPG
  P1b  -> equipment.JPG
  P1c  -> TSMCFab.JPG
  P2   -> kla3.webp
  P3   -> HVMflow.jpg

Fab Work Flow (hiện ở mục Description/Overview của mỗi phase):
  FW1  -> TD.jpg
  FW2  -> Metrology.JPG
  FW3  -> OverallPDK.JPG
  FW4  -> RnDlot.JPG  (đổi tên: bỏ ký tự & cho an toàn URL)
  FW5  -> foundryservice.jpg   (V2 gõ sai "foudryservice" -> đã sửa đúng)

Lưu ý:
- Phải mở site qua web server (không phải file://) để ảnh + dữ liệu tải được.
- Nếu một ảnh thiếu, ô ảnh sẽ hiện khung nét đứt báo "Không tải được ảnh",
  phần chữ vẫn hiển thị bình thường.
- Muốn đổi tên file / chú thích: sửa field "image" và "image_caption"
  trong data/flow.json (TT) và js/fab-data.js (FW).
